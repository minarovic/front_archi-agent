# Core dependencies for Pydantic Graph + Pydantic AI approach
# pydantic-ai includes pydantic-graph (verified via official docs)
pydantic-ai>=1.0.0  # Includes pydantic-graph for orchestration
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0
openai>=1.0.0
databricks-sdk>=0.73.0

# FastAPI Backend (Phase 3)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-multipart>=0.0.6
httpx>=0.27.0  # For TestClient

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Total dependency size: ~15MB (vs 330MB LangGraph) = 95% reduction

# Legacy dependencies (for original LangGraph notebooks - tool0/1/2/3_demo.ipynb)
# Uncomment if you need to run original versions:
# langgraph>=0.2.0
# langchain>=0.3.0
# langchain-openai>=0.2.0
# langchain-anthropic>=0.2.0
